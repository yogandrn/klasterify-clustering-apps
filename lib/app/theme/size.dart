import 'package:get/get.dart';

final double maxWidth = Get.width.roundToDouble();
final double maxHeight = Get.height.roundToDouble();

final double radiusSmall = (maxHeight / 200).roundToDouble();
final double radiusMedium = (maxHeight / 80).roundToDouble();
final double radiusLarge = (maxHeight / 60).roundToDouble();

final double marginSmall = (maxHeight / 108).roundToDouble();
final double marginMedium = (maxHeight / 72).roundToDouble();
final double marginLarge = (maxHeight / 42).roundToDouble();
